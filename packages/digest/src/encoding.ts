/**
 * Base64url encoding and decoding (RFC 4648 §5).
 *
 * Universal digest encoding across the ecosystem. All 256-bit digests
 * encode to 43-character base64url strings with no padding.
 *
 * @since 0.1.0
 * @category encoding
 */
