/**
 * Core type contracts for the digest package.
 *
 * Pure TypeScript types — no Effect dependency. These define the
 * shapes used by both the core layer and the schema layer.
 *
 * @since 0.1.0
 * @category contracts
 */
