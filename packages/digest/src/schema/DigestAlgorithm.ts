/**
 * Supported durable digest algorithms.
 *
 * `"blake3-256"` is the primary algorithm for all content-addressing.
 * `"sha256"` is the secondary for secrets and external compatibility.
 *
 * @since 0.1.0
 * @category schemas
 */
