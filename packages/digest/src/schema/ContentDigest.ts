/**
 * Algorithm-tagged digest pair for self-describing integrity checks.
 *
 * Combines a {@link DigestAlgorithm} with a {@link Digest256} value
 * to produce a portable, self-describing content digest.
 *
 * @since 0.1.0
 * @category schemas
 */
