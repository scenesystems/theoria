/**
 * Schema type contract tests (Effect layer).
 *
 * Verifies:
 * - Digest256 pattern validation (43-char base64url)
 * - ContentDigest encode/decode round-trip
 * - DigestAlgorithm literal union
 * - Invalid input rejection
 */
