/**
 * Canonical domain literal for the Distribution domain. Used as the
 * discriminator in `DistributionDomainSchema` and as the ownership
 * key in domain-crossing pipelines.
 *
 * @since 0.1.0
 * @category contracts
 */
export const DistributionDomainContract = "Distribution"
