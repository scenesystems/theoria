/**
 * Names unstable integration boundaries without supplying implementations.
 *
 * @remarks
 * This subpath is experimental: its exports may change or be removed in a
 * minor release while the package remains pre-1.0.
 *
 * @since 0.1.0
 * @module
 */

/**
 * String labels that identify the package's experimental boundaries.
 *
 * @remarks
 * These labels do not provide implementations or imply that corresponding
 * schemas or services are available.
 *
 * @since 0.1.0
 * @category experimental
 */
export const ExperimentalSeams = ["VariantSchema", "Machine", "Persistence"]
