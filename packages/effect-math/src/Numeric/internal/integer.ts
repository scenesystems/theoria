/**
 * Numeric integer kernels.
 *
 * @since 0.3.0
 * @category internal
 */

/**
 * Deterministic signed 32-bit multiplication.
 *
 * @since 0.3.0
 * @category internal
 */
export const imul: (left: number, right: number) => number = Math.imul
