/**
 * Ensemble — run multiple optimizers and select the parameter set with the
 * highest evaluation score.
 *
 * @since 0.1.0
 * @category constructors
 */
export * from "../optimizers/Ensemble/index.js"
