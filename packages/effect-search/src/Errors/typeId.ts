/**
 * Module-local symbol carried by constructed public search errors.
 * It identifies in-memory instances and is neither global nor serialized by the schemas.
 *
 * @since 0.1.0
 * @category symbols
 */
export const SearchErrorTypeId = Symbol("effect-search/SearchErrorTypeId")
