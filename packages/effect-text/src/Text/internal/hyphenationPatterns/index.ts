/**
 * Checked-in hyphenation pattern sources.
 *
 * @since 0.2.0
 */
import { deHyphenationPatternSource as deHyphenationPatternSourceData } from "./de.js"
import { enGbHyphenationPatternSource as enGbHyphenationPatternSourceData } from "./enGb.js"
import { enUsHyphenationPatternSource as enUsHyphenationPatternSourceData } from "./enUs.js"
import { esHyphenationPatternSource as esHyphenationPatternSourceData } from "./es.js"
import { frHyphenationPatternSource as frHyphenationPatternSourceData } from "./fr.js"

/**
 * Checked-in hyphenation pattern source for `de`.
 *
 * @since 0.2.0
 * @category internals
 */
export const deHyphenationPatternSource = deHyphenationPatternSourceData

/**
 * Checked-in hyphenation pattern source for `en-gb`.
 *
 * @since 0.2.0
 * @category internals
 */
export const enGbHyphenationPatternSource = enGbHyphenationPatternSourceData

/**
 * Checked-in hyphenation pattern source for `en-us`.
 *
 * @since 0.2.0
 * @category internals
 */
export const enUsHyphenationPatternSource = enUsHyphenationPatternSourceData

/**
 * Checked-in hyphenation pattern source for `es`.
 *
 * @since 0.2.0
 * @category internals
 */
export const esHyphenationPatternSource = esHyphenationPatternSourceData

/**
 * Checked-in hyphenation pattern source for `fr`.
 *
 * @since 0.2.0
 * @category internals
 */
export const frHyphenationPatternSource = frHyphenationPatternSourceData
