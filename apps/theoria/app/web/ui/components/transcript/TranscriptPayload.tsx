import type { ComponentProps } from "react"

import { PayloadBlock } from "../detail/PayloadBlock.js"

export const TranscriptPayload = (props: ComponentProps<typeof PayloadBlock>) => <PayloadBlock {...props} />
