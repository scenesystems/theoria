{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun run --filter @theoria/theoria-app build:web",
    "watchPatterns": ["/apps/theoria/**", "/packages/**"]
  },
  "deploy": {
    "startCommand": "cd apps/theoria && bun run server.ts",
    "healthcheckPath": "/api/health/live",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
