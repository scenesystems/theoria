diff --git a/floating-ui-react/hooks/useHoverInteractionSharedState.d.mts b/floating-ui-react/hooks/useHoverInteractionSharedState.d.mts
index 43f79639f70f8027c951b06a7b2edd26044bacca..43f36461efa8c637aacdf04073ce8996e5f9f3a0 100644
--- a/floating-ui-react/hooks/useHoverInteractionSharedState.d.mts
+++ b/floating-ui-react/hooks/useHoverInteractionSharedState.d.mts
@@ -15,6 +15,7 @@ export declare class HoverInteraction {
   openChangeTimeout: Timeout;
   restTimeout: Timeout;
   handleCloseOptions: SafePolygonOptions | undefined;
+  consumers: number;
   constructor();
   static create(): HoverInteraction;
   dispose: () => void;
diff --git a/floating-ui-react/hooks/useHoverInteractionSharedState.d.ts b/floating-ui-react/hooks/useHoverInteractionSharedState.d.ts
index 4c9fa62b9c9e17e698a1c4451d19e483345289f4..ca096f107d7145b6e2400c31d10f89538bb30dc7 100644
--- a/floating-ui-react/hooks/useHoverInteractionSharedState.d.ts
+++ b/floating-ui-react/hooks/useHoverInteractionSharedState.d.ts
@@ -15,6 +15,7 @@ export declare class HoverInteraction {
   openChangeTimeout: Timeout;
   restTimeout: Timeout;
   handleCloseOptions: SafePolygonOptions | undefined;
+  consumers: number;
   constructor();
   static create(): HoverInteraction;
   dispose: () => void;
diff --git a/floating-ui-react/hooks/useHoverInteractionSharedState.js b/floating-ui-react/hooks/useHoverInteractionSharedState.js
index 6d19dd2e8565db0a8afca8c428c1437b92ed8238..7113e0d73fa188010a5f85afde24503e8d0c89e5 100644
--- a/floating-ui-react/hooks/useHoverInteractionSharedState.js
+++ b/floating-ui-react/hooks/useHoverInteractionSharedState.js
@@ -32,6 +32,7 @@ class HoverInteraction {
     this.openChangeTimeout = new _useTimeout.Timeout();
     this.restTimeout = new _useTimeout.Timeout();
     this.handleCloseOptions = undefined;
+    this.consumers = 0;
   }
   static create() {
     return new HoverInteraction();
@@ -40,8 +41,17 @@ class HoverInteraction {
     this.openChangeTimeout.clear();
     this.restTimeout.clear();
   };
+  // The state is shared by every trigger and the popup of one root. Its
+  // timers belong to whichever consumer last started them, so a single
+  // consumer unmounting must not clear them; only the last one to leave does.
   disposeEffect = () => {
-    return this.dispose;
+    this.consumers += 1;
+    return () => {
+      this.consumers -= 1;
+      if (this.consumers === 0) {
+        this.dispose();
+      }
+    };
   };
 }
 exports.HoverInteraction = HoverInteraction;
diff --git a/floating-ui-react/hooks/useHoverInteractionSharedState.mjs b/floating-ui-react/hooks/useHoverInteractionSharedState.mjs
index 835ebe5fb5caa092bc5be3352168513dd1ee444d..3c24838a2be9021d28a8750852c821deaf080586 100644
--- a/floating-ui-react/hooks/useHoverInteractionSharedState.mjs
+++ b/floating-ui-react/hooks/useHoverInteractionSharedState.mjs
@@ -19,6 +19,7 @@ export class HoverInteraction {
     this.openChangeTimeout = new Timeout();
     this.restTimeout = new Timeout();
     this.handleCloseOptions = undefined;
+    this.consumers = 0;
   }
   static create() {
     return new HoverInteraction();
@@ -27,8 +28,17 @@ export class HoverInteraction {
     this.openChangeTimeout.clear();
     this.restTimeout.clear();
   };
+  // The state is shared by every trigger and the popup of one root. Its
+  // timers belong to whichever consumer last started them, so a single
+  // consumer unmounting must not clear them; only the last one to leave does.
   disposeEffect = () => {
-    return this.dispose;
+    this.consumers += 1;
+    return () => {
+      this.consumers -= 1;
+      if (this.consumers === 0) {
+        this.dispose();
+      }
+    };
   };
 }
 const pointerEventsMutationOwnerByScopeElement = new WeakMap();
