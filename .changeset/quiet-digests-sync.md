---
"@scenesystems/digest": minor
---

Add `digestSchemaValueWithByteLimitSync`, a bounded synchronous `Either` API that shares the existing Schema encoding, strict JCS traversal, UTF-8, incremental digest, tagged-output, and closed error laws without requiring an Effect runtime.
