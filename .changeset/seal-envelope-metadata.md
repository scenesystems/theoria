---
"@scenesystems/seal": minor
---

Add additive envelope key metadata with optional `keyId` and `keyVersion` on `SealedEnvelope`, plus the `seal(..., metadata)` transport workflow, backward-compatible unsealing, README guidance, and key-rotation example coverage.
