---
"@scenesystems/sign": patch
---

The build no longer writes `dist/verifier-descriptor.json`; the release-descriptor scripts that produced it were removed together with the release-snapshot governance. The published code is unchanged.
