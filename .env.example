# Theoria app runtime configuration
# Copy to .env and fill in values for local or production-like runs.

# App server
PORT=3876
BUILD_SHA=dev-local
# Release stage gating (development | preview | production). Authoritative when
# set; otherwise derived from RAILWAY_ENVIRONMENT_NAME / NODE_ENV.
RELEASE_STAGE=development
